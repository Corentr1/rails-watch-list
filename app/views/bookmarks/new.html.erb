<div class="container">
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <%= f.input :comment %>
    <%= f.input :movie_id, collection: @movies %>
    <%= f.submit class: "btn btn-primary"%>
  <% end %>
</div>
